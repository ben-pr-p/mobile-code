import React from 'react'
import type { ToolCallProps } from './types'
import { DefaultToolCollapsed, DefaultToolExpanded } from './DefaultTool'

// TODO: Collapsed — "N items" with completed count; Expanded — rendered todo list
export const TodoToolCollapsed = DefaultToolCollapsed
export const TodoToolExpanded = DefaultToolExpanded
