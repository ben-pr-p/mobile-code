import React from 'react'
import type { ToolCallProps } from './types'
import { DefaultToolCollapsed, DefaultToolExpanded } from './DefaultTool'

// TODO: Collapsed — description + agent type badge; Expanded — sub-agent markdown response
export const TaskToolCollapsed = DefaultToolCollapsed
export const TaskToolExpanded = DefaultToolExpanded
