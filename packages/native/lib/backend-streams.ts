/**
 * @deprecated This module is retained for backwards compatibility during migration.
 * Use `globalDb` from `./global-db` and `getApi` from `./api` instead.
 */
export { globalDb } from './global-db';
export { getApi } from './api';
